export const BROWSER_CAPTURE_DOCS_HTML = "<p>Shiplet brings multiplayer Figma-style comments to anything you can open in a browser. Leave feedback directly on the work so teammates and AI agents can see what needs changing and act on it.</p>\n<p>Use the <a href=\"/docs/embed\">live widget</a> on sites your team controls. For other browser work, <strong>capture what you see</strong> and comment on the shared visual copy. Captures preserve signed-in app screens, PDFs, documents, canvas content, embedded frames, and pages that cannot run a widget. They are still images, not live replicas.</p>\n<h2>Start without installing anything</h2>\n<p>1. Open <a href=\"https://shiplet.cc/capture\">Capture browser work</a>.</p>\n<p>2. Choose <strong>Capture a tab or window</strong> and select the work in the browser&#x27;s picker. You can also upload or paste a PNG, JPEG, or WebP screenshot.</p>\n<p>3. Review the image. Drag over sensitive areas to redact them, or enter a rectangle under <strong>Redact using coordinates</strong>. Only the edited image is shared.</p>\n<p>4. Sign in, choose a workspace, confirm sharing, and select <strong>Create shared review</strong>.</p>\n<p>5. Choose <strong>Annotate</strong>, click a point in the capture, and leave a comment. Share the review link with a teammate who has workspace access.</p>\n<p>Screen sharing captures one frame and immediately stops; there is no background recording. Upload/paste works on browsers and devices without screen sharing. Images must fit within 20 megapixels, 16,384 pixels per side, and a 6 MB PNG after conversion. Capture a smaller area if an image exceeds those limits.</p>\n<h2>Install the browser companion</h2>\n<p>The Chromium companion captures the visible area of your current tab directly, without changing the website or requiring its owner to install anything.</p>\n<p>1. <a href=\"https://shiplet.cc/downloads/shiplet-browser-companion.zip\">Download the browser companion</a> and unzip it to a folder you will keep.</p>\n<p>2. Open Chrome&#x27;s <code>chrome://extensions</code> (or Edge&#x27;s <code>edge://extensions</code>). Turn on <strong>Developer mode</strong>, choose <strong>Load unpacked</strong>, and select the extracted folder containing <code>manifest.json</code>.</p>\n<p>3. Pin <strong>Shiplet Browser Companion</strong> to your toolbar.</p>\n<p>4. Open the work, click the companion, then <strong>Capture this tab</strong>. Review and redact the local preview. Choose <strong>Continue to Shiplet</strong> to select a workspace and confirm publishing.</p>\n<p>This download is the release distribution; it does not require a store listing. Chrome/Edge desktop 116 or newer is required. Updates are manual: download the new archive, replace the extracted files, and choose <strong>Reload</strong> on the extensions page. Remove the extension there to uninstall it. Firefox, Safari and mobile users can use the web capture/upload path above.</p>\n<h2>What teammates and agents receive</h2>\n<p>Each capture creates an organization-visible Shiplet with the reviewed image, capture timestamp, pixel dimensions, and optional original page URL. Comments, numbered pins, replies, resolution and reopening use Shiplet&#x27;s normal shared review workflow. Teammates do not need the original site&#x27;s account or cookies. This also means that sharing a capture shares its visible information.</p>\n<p>Agents read the same feedback through <a href=\"/docs/code-mode-mcp\">Code Mode MCP</a> and the <a href=\"/docs/review-feedback\">review feedback API</a>. The artifact includes <code>capture.png</code> and <code>capture.json</code>; comment screenshots preserve raster pixels. Agents can inspect the requested change and act in a connected repository or tool when authorized. A capture does not grant access to edit the original site.</p>\n<h2>Privacy and browser restrictions</h2>\n<p>The companion has temporary access to the tab you explicitly invoke it on and uses no persistent access to third-party sites. It does not read page DOM, cookies, passwords, network requests, or browsing history. Its only content script runs on Shiplet&#x27;s capture page to deliver the preview. It executes bundled code only, keeps pending images in browser session memory for at most ten minutes, and consumes each transfer once. Closing its preview or destination tab discards the pending transfer. Captured images are not automatically uploaded.</p>\n<p>Pixels can contain sensitive information, including visible input values. Review and redact before sharing. Black redactions are baked into the PNG; Shiplet never receives the original under those rectangles. A preview&#x27;s reset button only restores the local image. URL query strings and fragments are removed, and non-web URLs (including local file paths) are omitted. Clear the optional source URL when the URL path itself is private.</p>\n<p>Browser-protected screens, enterprise policies, DRM video, and operating-system capture restrictions can block or blank pixels. Shiplet cannot bypass those restrictions. Use a screenshot or exported image you are permitted to share. For long pages, capture the relevant region or upload a full-page screenshot. Captured links, forms and video are pixels; use the live widget when teammates need to interact with the running site.</p>\n<h2>Paste this into your agent</h2>\n<pre><code>Help me set up Shiplet browser capture using https://shiplet.cc/docs/browser-capture.\nDownload https://shiplet.cc/downloads/shiplet-browser-companion.zip and extract\nit into a persistent folder. Show me how to load that folder from my Chromium\nbrowser&#x27;s extensions page using Developer mode &gt; Load unpacked, then pin it.\nDo not alter browser security policy or install it silently.\n\nWalk me through capturing one page, redacting sensitive pixels locally, and\npublishing the reviewed image to my chosen Shiplet workspace. Verify that a\nsecond authorized reviewer can pin a comment, reply and resolve it, and that\nthe same feedback is available through Shiplet&#x27;s API or Code Mode MCP.\nUse https://shiplet.cc/capture with image upload if extension installation or\nscreen capture is unavailable. Do not upload private content without my review.</code></pre>\n<p>For self-hosted Shiplet, build the companion from source after changing the trusted origin in <code>browser-companion/config.js</code>, the exact capture-page match in <code>manifest.json</code>, and the fallback link in <code>popup.html</code>. Rebuild with <code>node scripts/build-browser-companion.mjs</code>. Keep the match limited to your Shiplet origin; never add all-sites access.</p>";
